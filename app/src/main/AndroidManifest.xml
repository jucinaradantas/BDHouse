<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifsp.bdhouse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_bdh_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_bdh_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BDHActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VisitaActivity"
            android:parentActivityName=".BDHActivity" />
        <activity android:name=".Visita_EditarActivity" />
        <activity android:name=".Visita_InserirActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".UsuarioActivity" />
        <activity android:name=".AlarmeActivity" />
        <activity android:name=".LuzesActivity" />
        <activity android:name=".EletrodomesticoActivity" />
        <activity android:name=".PortaoActivity" />
        <activity android:name=".TetoActivity" />
        <activity android:name=".SoloActivity" />
        <activity android:name=".ArActivity"></activity>
    </application>

</manifest>